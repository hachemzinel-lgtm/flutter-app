import 'package:go_router/go_router.dart';

/// Placeholder for any extra auth-flow routes that were previously defined
/// in a separate module. Returns an empty list so the spread in
/// [buildAppRouter] resolves without error.
List<RouteBase> buildSimpleAuthRoutes() {
  return const <RouteBase>[];
}
