import 'package:flutter/material.dart';

import 'package:flutter_application_1/providers/provider_home_screen.dart';

class WorkProviderHomeScreen extends StatelessWidget {
  const WorkProviderHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: replace this wrapper once the provider home screen is moved into
    // the role-based home feature module.
    return const ProviderHomeScreen();
  }
}
